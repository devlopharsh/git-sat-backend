apiVersion: apps/v1
kind: Deployment
metadata:
  name: git-sat-backend
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: app
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: staging
